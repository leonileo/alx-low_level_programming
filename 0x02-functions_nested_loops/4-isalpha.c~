#include "main.h"

/**
 * _isalpha - check and print the alphabet character
 * @c : is the character to be checked
 *
 * Return: 1 if the charachter is alphabet letter otherwise 0
*/

int _isalpha(int c)
{
if (c >= 65 && c <= 90 || c >= 97 && c <= 122)
{
return (1);
}
return (0);
}
